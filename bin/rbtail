#!/usr/bin/env ruby
#
# Simple 'tail -f' example.
# Usage example:
#   rbtail.rb /var/log/messages

ENV["RACK_ENV"] ||= "development"

require "rubygems"
#require "bundler/setup"
#require "bundler/index"

#Bundler.setup
#Bundler.require(:default, ENV["RACK_ENV"].to_sym)

require_relative '../rbtail_ex'
